# ==============================================================
# PAPI HAIR DESIGN – Booking System
# Premenné prostredia (skopíruj do .env a vyplň hodnoty)
# ==============================================================

# --- Supabase (náhrada za Firebase Functions na Spark; rezervácie, sync, webauthn…) ---
# Supabase Dashboard: https://supabase.com/dashboard → projekt → Settings → API
#
# VITE_SUPABASE_URL = Project URL (bez lomítka na konci)
# VITE_SUPABASE_PUBLISHABLE_KEY = kľúč "anon" / "public" (NIE service_role)
# Vo frontende sa používajú tieto dve premenné; serverový SUPABASE_KEY sem nepatrí.
#
# Project ref z URL (hrkwqdvfeudxkqttpgls) – Supabase Dashboard → Settings → API
VITE_SUPABASE_PROJECT_ID=hrkwqdvfeudxkqttpgls
VITE_SUPABASE_URL=https://hrkwqdvfeudxkqttpgls.supabase.co
# Publishable Key alebo Anon Key (Legacy) – oba fungujú (Dashboard → API)
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-or-anon-key

# Voliteľné pre CLI (migrácie a supabase link/push):
# Direct connection – projekt hrkwqdvfeudxkqttpgls (heslo len lokálne, nikdy do gitu):
# SUPABASE_DB_URL=postgresql://postgres:HESLO@db.hrkwqdvfeudxkqttpgls.supabase.co:5432/postgres
# Session pooler (IPv4 napr. Vercel): postgresql://postgres.hrkwqdvfeudxkqttpgls:[YOUR-PASSWORD]@aws-1-eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true
# Host: db.hrkwqdvfeudxkqttpgls.supabase.co | Port: 5432 | Database: postgres | User: postgres
# SUPABASE_ACCESS_TOKEN=token z Supabase Dashboard → Account → Access Tokens

# --- Firebase (phd-booking – predvyplnené; môžeš prepísať env premennými) ---
# Firebase Console → Project Settings → General → Your apps → Web app
# Ak sú nastavené, auth sa rieši cez Firebase; inak Supabase Auth.
# Analytics sa inicializuje vždy (projekt phd-booking). Více: docs/FIREBASE-AUTH-SETUP.md
# VITE_FIREBASE_API_KEY=
# VITE_FIREBASE_AUTH_DOMAIN=
# VITE_FIREBASE_PROJECT_ID=
# VITE_FIREBASE_APP_ID=
# VITE_FIREBASE_STORAGE_BUCKET=
# VITE_FIREBASE_MESSAGING_SENDER_ID=
# VITE_FIREBASE_MEASUREMENT_ID=

# Google Sign-in: Web client ID (Firebase Console → Authentication → Sign-in method → Google → Web SDK configuration).
# Voliteľné – ak nie je nastavené, Firebase používa predvolený OAuth klient projektu.
# Pre web app stačí tento Web client ID; SHA-1 fingerprint je len pre Android apps.
# VITE_GOOGLE_OAUTH_CLIENT_ID=1054453277711-pq5qskmihcjivdg7dbjv485eb85gd49i.apps.googleusercontent.com

# --- Firebase App Check (debug token pre dev / testovanie) ---
# Firebase Console → App Check → Manage debug tokens. Používa sa na localhost / emulatoroch / CI.
# VITE_APP_CHECK_DEBUG_TOKEN=FFAD7D74-5F9F-4214-8E74-8F19C3BD643E

# --- reCAPTCHA v3 (neviditeľné, verejná rezervácia) ---
# Site key – frontend (.env). Secret key NIKDY sem – len Firebase Console → Functions → env: RECAPTCHA_SECRET.
# VITE_RECAPTCHA_SITE_KEY=your-recaptcha-site-key

# --- Vercel (voliteľné – pre automatizované deploye) ---
# VERCEL_TOKEN sa nepoužíva priamo v aplikácii, ale pre automatizované deploye cez CLI.
# Token sa nastavuje ako env premenná vo Vercel prostredí pomocou skriptu:
#   .\scripts\set-vercel-token-env.ps1
# Potom môžeš používať automatizované deploye:
#   .\scripts\deploy-vercel.ps1
# Viac informácií: docs/VERCEL-DIAGNOSTICS.md

# ==============================================================
# POZNÁMKY:
# - Nikdy nepridávaj VITE_SUPABASE_SERVICE_ROLE_KEY do .env!
#   Service role key patrí iba do Supabase edge function secrets.
# - Tento súbor (.env.example) je bezpečné commitnúť do gitu.
# - Súbor .env NIKDY necommituj (je v .gitignore).
# ==============================================================
